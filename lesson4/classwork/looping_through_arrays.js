let animals = ["cats", "dogs", "lion", "rabbit", "turtle"];
for (let i = 0; i < animals.length; i++) {  // i goes through 0 - length-1
    let animal = animals[i];  // Get the item at this index
    console.log(animal);
}

let numbers = [4, 8, 15, 16, 23, 42];

for (let i = 0; i < numbers.length; i++) {
    let number = numbers[i];
    console.log("Number at index", i, "is", number);
}