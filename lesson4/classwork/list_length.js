let fruits = [ "watermelon", "grapes", "apple", "cherry", "date", "passionfruit"];

let length = fruits.length;
console.log ("number of fruits:", length );

let last_fruits = fruits[length -1];
console.log ("last_fruits:", last_fruits);